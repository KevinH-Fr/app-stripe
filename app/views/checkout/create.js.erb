const stripe = Stripe('<%= Rails.application.credentials[:stripe][:public] %>');
const checkoutButton = document.getElementById('checkout-button');

checkoutButton.addEventListener('click', () => {
  stripe.redirectToCheckout({
    sessionId: '<%= @session.id %>'
  })
  .then(function(result) {
    // Handle any redirection or processing errors here if needed
  })
  .catch(function(error) {
    // Handle any other errors that may occur during the redirect
  });
});

//console.log("call create js")